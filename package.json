{
  "private": true,
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.12.2",
    "@typescript-eslint/parser": "^8.12.2",
    "eslint": "^9.14.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.3.3"
  },
  "scripts": {
    "format": "prettier --write .",
    "lint": "eslint . --ext .ts,.tsx --max-warnings=0",
    "dev:functions": "cd functions && firebase emulators:start --only functions,firestore",
    "dev:web": "cd web && npm run dev",
    "dev": "npm-run-all -p dev:functions dev:web"
  }
}
